class Myclass{
public static void main(String args[])
{
double sum=0,avg;
for(int i =0;i<args.length;i++)
{
avg=Integer.parseInt(args.[i]);
sum=sum+arg;
}
double avg= Sum(args.length);
System.out.println("SUM of "+ args.length+" command line argument is "+sum);
System.out.println("average of "+" command line argument" +avg);
}
}
